# Getting Started with running the application

1. To get started, run `yarn` or `npm install` inside the directory to install the packages.
2. Now run, `yarn start` or `npm start` to start the development server.
3. Visit `localhost:3000` to view the application.
